import {View, Text} from 'react-native';
import React from 'react';

const Search = () => {
  return (
    <View
      style={{
        flexDirection: 'row',
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      }}>
      <Text>Search</Text>
    </View>
  );
};

export default Search;
